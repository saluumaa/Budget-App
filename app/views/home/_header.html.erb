<nav class="navbar navbar-expand-lg navbar-dark nav-container">
  <div class="container-fluid">
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <% if current_page?(categories_path) %>
          <li class="nav-item">
            <%= link_to image_tag("back icon.png", class: "nav-icon"), root_path, class: "nav-link active" %>
          </li>
          <li class="nav-item text-center justify-content-center">
            <h2 class="nav-title">Categories</h2>
          </li>

        <% end %>

        <% if current_page?(new_category_path) %>
          <li class="nav-item">
            <%= link_to image_tag("back icon.png", class: "nav-icon"), categories_path, class: "nav-link active" %>
          </li>
          <li class="nav-item text-center justify-content-center">
            <h2 class="nav-title">AddCategory</h2>
          </li>
          <%end%>

         <% if defined?(@category) && @category.persisted? %>
          <li class="nav-item">
            <%= link_to image_tag("back icon.png", class: "nav-icon"), categories_path, class: "nav-link active" %>
          </li>
            <li class="nav-item text-center justify-content-center">
              <h2 class="nav-title">Transactions</h2>
            </li>
          <% end %>
           

        <% if current_page?(root_path)%>
            <li class="nav-item">
                <%= link_to 'Blog App', root_path, class: "nav-link active" %>
            </li>
        <% end %>
      </ul>
      <ul class="navbar-nav ml-auto">
        <% if user_signed_in? %>
          <li class="nav-item">
            <%= link_to 'Sign Out', destroy_user_session_path, data: { turbo_method: :delete }, class: "nav-link text-center" %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to 'Sign In', new_user_session_path, class: "nav-link text-center" %>
          </li>
          <li class="nav-item">
            <%= link_to 'Sign Up', new_user_registration_path, class: "nav-link text-center" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
